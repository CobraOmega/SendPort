# SMTP (dev: MailHog) or SES later
SMTP_HOST=localhost
# for SES later use SMTP_HOST=email-smtp.<region>.amazonaws.com

SMTP_PORT=1025
# for SES later use SMTP_PORT=587

SMTP_USER="your@gmail.com"
SMTP_PASS="app-password (gmail)"
# for SES later use SMTP_USER & SMTP_PASS are SES SMTP credentials

DEFAULT_FROM=no-reply@example.com

# App server
APP_HOST=0.0.0.0
APP_PORT=8000

# Security: simple API key for demo (rotate in prod)
SERVICE_API_KEY=super-secret-dev-key
